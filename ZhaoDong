class PianoKey:
    def __init__(self, note, is_black):
        self.note = note
        self.is_black = is_black

    def press(self):
        print(f"Playing {self.note}")

# Create a piano key
c = PianoKey("C", False)

# press the piano keys
c.press()
